{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install",
    "watchPatterns": ["server/**", ".npmrc", "package.json", "package-lock.json"]
  },
  "deploy": {
    "startCommand": "cd server && node index.js",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
